<h1>Editing attack_spell</h1>

<%= form_for [:admin, @attack_spell] do |f| %>
  <%= render :partial => 'form', locals: {f: f} %>
<% end %>

<%= link_to 'Show', :action => 'show', :id => @attack_spell %> |
<%= link_to 'Back', admin_attack_spells_path %>
