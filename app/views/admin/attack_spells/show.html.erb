<% for column in AttackSpell.content_columns %>
<p>
  <b><%= column.human_name %>:</b> <%=h @attack_spell.send(column.name) %>
</p>
<% end %>

<%= link_to 'Edit', :action => 'edit', :id => @attack_spell %> |
<%= link_to 'Back', admin_attack_spells_path %>
