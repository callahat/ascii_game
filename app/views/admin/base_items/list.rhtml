<h1>Listing base_items</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Equip Location</th>
    <th>Dfn</th>
    <th>Dam</th>
    <th>Price</th>
    <th>Race Bdy Type</th>
  </tr>
  
<% for base_item in @base_items %>
  <tr>
    <td><%= base_item.name %></td>
    <td>
    <% if base_item.equip_loc.nil? %>
    N/A
    <% else %>
    (<%= base_item.equip_loc %>) - <%= SpecialCode.get_text('equip_loc',base_item.equip_loc) %>
    <% end %>
    </td>
    <td><%= base_item.dfn_mod %></td>
    <td><%= base_item.dam_mod %></td>
    <td><%= base_item.price %></td>
    <td>
    <% if base_item.race_body_type.nil? %>
    N/A
    <% else %>
    (<%= base_item.race_body_type %>) - <%= SpecialCode.get_text('race_body_type',base_item.race_body_type) %>
    <% end %>
    </td>
    <td><%= link_to 'Show', :action => 'show', :id => base_item %></td>
    <td><%= link_to 'Edit', :action => 'edit', :id => base_item %></td>
    <td><%= link_to 'Destroy', { :action => 'destroy', :id => base_item }, :confirm => 'Are you sure?', :method => :post %></td>
  </tr>
<% end %>
</table>

<%= will_paginate @base_items %> 

<br />

<%= link_to 'New base_item', :action => 'new' %>
